spring:
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.eu.mailgun.org
    username: oris@mail.kaanf.com

jwt:
  expiration-minutes: 1000

nginx:
  require-proxy: false

oris:
  email:
    url: "http://localhost:8080"
  rate-limit:
    ip:
      apply-limit: false
  web-socket:
    allowed-origin: "http://localhost:8080"